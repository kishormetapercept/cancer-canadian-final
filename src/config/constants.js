module.exports = {
  UPLOAD_DIR: 'uploads/',
  OUTPUT_DIR: 'output',
  XSLT_OUTPUT_DIR: 'xslt_output',
  MAX_FILE_SIZE: 100 * 1024 * 1024, // 100MB
  ALLOWED_MIME_TYPES: ['application/zip'],
  ALLOWED_EXTENSIONS: ['.zip']
};
